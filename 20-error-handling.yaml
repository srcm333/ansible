- name: demo on shell vs command
  hosts: local
  connection: local
  tasks:
    - name: command test
      shell: cat /etc/passwd | grep root
      register: users_output

    - name: print users_output
      debug:
        msg: "{{ users_output }}"
    
    - name: list files 
      shell: ls -lrth > /tmp/files.txt