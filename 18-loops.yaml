- name: install packages
  hosts: frontend
  become: yes
  tasks:
    - name: install packages
      package: 
        name: "{{ item.name }}"
        state: "{{ item.state }}"
      loop:
        - { name: "nginx", state: "present" }
        - { name: "mysql", state: "absent" }
        - { name: "mongodb-mongosh", state: "absent" }
        - { name: "zip", state: "present" }
        - { name: "git", state: "absent" }


  