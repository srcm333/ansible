- name: install and start nginx
  hosts: frontend
  tasks:
  - name: install nginx
    ansible.builtin.ping:
      name: nginx
      state: present

  - name: start nginx
    service:
      name: nginx
      state: started
      enabled: yes